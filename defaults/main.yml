---
certificate_path: tls

drupal_username: drupalcore
drupal_passwd: drupalpass
drupal_revision: HEAD

NFS_VIRTUAL_IP:
MySQL_VIRTUAL_IP:
ROUTER_PUBLIC_IP:
certificate_prefix:
drupal_database:
mailname: